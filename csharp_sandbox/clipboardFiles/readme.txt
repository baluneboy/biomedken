
8. If what got this started was click on:
   -- directory, then do recursive search like we've been doing
   -- file, then parse file into stub for search pattern and put dirname into textbox for dir to be searched

3. Two modes: (default is file search, other is folder search).

5. Drop down (menu or what control?) to list commonly used search expressions.

6. Ability to add current search to the "commonly used search expressions" [menu?].
